apiVersion: v1
kind: Service
metadata:
  name: {{ service_config.name }}-svc
  namespace: {{ service_config.namespace }}
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ service_config.service.namespace }}
  type: nodePort
  selector:
    app: {{ service_config.name }}